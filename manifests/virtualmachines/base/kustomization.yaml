---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared Fedora VM template. Per-VM overlays patch identity fields
# (name, DataVolume, cloud-init secret references).
resources:
  - virtualmachine.yaml
